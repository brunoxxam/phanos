# phanos
local DevSecOps security gate leveraging Abstract Syntax Tree (AST) pre-filtering and local LLMs (Ollama) to detect behavioral intent, deobfuscate scripts, and block malicious supply chain attacks in open-source dependencies.
